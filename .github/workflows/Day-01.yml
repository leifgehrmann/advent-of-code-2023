name: Day-01

on:
  workflow_dispatch:
  push:
    paths:
      - '**01*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: php-actions/composer@v6
      - name: Run
        run: php src/main.php 01 -b
